import { defineStore } from 'pinia'

export const useAppStore = defineStore('app', {
  state: () => ({
    // Estado inicial da aplicação
  }),
  getters: {
    // Getters da aplicação
  },
  actions: {
    // Actions da aplicação
  }
})

